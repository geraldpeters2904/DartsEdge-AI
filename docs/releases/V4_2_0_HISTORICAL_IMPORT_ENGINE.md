# DartsEdge v4.2.0 — Historical Import Engine

Adds resume-safe orchestration, progress detection from stable MODUS mappings,
attempt tracking and a persistent audit trail.

The engine never bypasses validation, Collector Preview or explicit commit.
No migration is required.
